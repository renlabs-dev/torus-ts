node-linker=hoisted
link-workspace-packages=true

publish-branch=dev

# Security: Ignore scripts by default to prevent supply chain attacks
# Scripts can still be run manually with: pnpm rebuild <package>
ignore-scripts=true
