import { SimpleBridgeForm } from "./simple-bridge-form";

/**
 * Render the simple bridge form UI.
 *
 * @returns A JSX element containing the simple bridge form.
 */
export function SimpleBridge() {
  return <SimpleBridgeForm />;
}