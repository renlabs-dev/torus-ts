import type { Node, Edge } from "@xyflow/react";

export const nodes: Node[] = [
  {
    id: "selector-1",
    type: "selector",
    data: { label: "Node Selector" },
    position: { x: 0, y: 0 },
  },
];

export const edges: Edge[] = [];
