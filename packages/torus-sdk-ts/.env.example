# Environment variables for chain tests
# Copy this file to .env.local and update with your values


# Chain RPC URL for testing
# Default: wss://api.testnet.torus.network
TEST_CHAIN_RPC_URL="wss://api.testnet.torus.network"

# Alternative endpoints:
# TEST_CHAIN_RPC_URL="wss://api.torus.network"          # Mainnet
# TEST_CHAIN_RPC_URL="wss://api.testnet.torus.network"  # Testnet
# TEST_CHAIN_RPC_URL="ws://localhost:9944"              # Local node

# Note: This value is used by chain integration tests in this package.
# Unit tests do not require it. Chain tests will perform real network requests.
